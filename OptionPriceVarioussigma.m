% computation parameter
N = 5000;  % number of timesteps

% option parameters
S0 = 10;      % initial stock price
K = 10;       % strike price
T = 1;       % expiry time
r = 0.02;      % interest rate
sigma = 0.1;  % volatility
mu = 0.05; %drift

optionPrice = AmericanPut(N,S0,K,T,r,sigma,mu);

% computation parameter
N = 5000;  % number of timesteps

% option parameters
S0 = 10;      % initial stock price
K = 10;       % strike price
T = 1;       % expiry time
r = 0.02;      % interest rate
sigma = 0.15;  % volatility
mu = 0.05; %drift

optionPrice = AmericanPut(N,S0,K,T,r,sigma,mu);

% computation parameter
N = 5000;  % number of timesteps

% option parameters
S0 = 10;      % initial stock price
K = 10;       % strike price
T = 1;       % expiry time
r = 0.02;      % interest rate
sigma = 0.2;  % volatility
mu = 0.05; %drift

optionPrice = AmericanPut(N,S0,K,T,r,sigma,mu);

% computation parameter
N = 5000;  % number of timesteps

% option parameters
S0 = 10;      % initial stock price
K = 10;       % strike price
T = 1;       % expiry time
r = 0.02;      % interest rate
sigma = 0.25;  % volatility
mu = 0.05; %drift

optionPrice = AmericanPut(N,S0,K,T,r,sigma,mu);

% computation parameter
N = 5000;  % number of timesteps

% option parameters
S0 = 10;      % initial stock price
K = 10;       % strike price
T = 1;       % expiry time
r = 0.02;      % interest rate
sigma = 0.3;  % volatility
mu = 0.05; %drift

optionPrice = AmericanPut(N,S0,K,T,r,sigma,mu);